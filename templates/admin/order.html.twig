{% extends '@!EasyAdmin/layout.html.twig' %}
{% block main %}
    <div>
        <p>{{ order.id }}</p>
        <p>{{ order.createdAt|date ('d/m/Y') }}</p>
    </div>

    {# REcup code du github de l'intervenant #}
{% endblock %}